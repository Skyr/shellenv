let mapleader = "#"

" Only enable on of the status line plugins
call janus#disable_plugin('vim-statline')
"call janus#disable_plugin('vim-powerline')

if filereadable(expand("~/.vimrc.before.local"))
    source ~/.vimrc.before.local
endif
